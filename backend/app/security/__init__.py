"""Security posture: the keys this install stands on, what it exposes,
and whether its credentials are being used by anyone else.

Everything here is measured from the running install rather than
asserted. A security page whose rows are always green is decoration.
"""
